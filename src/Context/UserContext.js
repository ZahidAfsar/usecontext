import { createContext } from "react";

// createContext creates a new object and we are storing that inside of our UserContext Variable
const UserContext = createContext();

export default UserContext;